// useQueries.ts — local data hooks (app uses localStorage, not a backend actor)
// All data operations go through useData.ts and useAuth.ts

export { useData } from "./useData";
export { useAuth } from "./useAuth";
